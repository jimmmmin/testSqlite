<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.sqlite.Mapper.ExampleMapper">

    <select id="selectStatus" resultType="com.example.sqlite.model.Status">
        SELECT status.subscription,
               status.timeout,
               status.minBatchSize
        FROM status
        WHERE id = (SELECT MAX(status.id) FROM status)
    </select>

    <insert id="insertStatus">
        INSERT INTO status (subscription, timeout, minBatchSize)
        VALUES (#{subscription}, #{timeout}, #{minBatchSize})
    </insert>


</mapper>